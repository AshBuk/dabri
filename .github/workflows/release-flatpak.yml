# Copyright (c) 2025 Asher Buk
# SPDX-License-Identifier: MIT

name: Release Flatpak

on:
  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+'  # Only stable tags (v1.0.0), not RC (v1.0.0-rc.1)
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-flatpak:
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: recursive  # shared-modules (Ayatana AppIndicator) used by the manifest

      - name: Install flatpak and flatpak-builder
        run: |
          sudo apt-get update
          sudo apt-get install -y flatpak flatpak-builder
          flatpak remote-add --user --if-not-exists flathub https://flathub.org/repo/flathub.flatpakrepo

      - name: Build Flatpak
        run: |
          echo "=== Building Flatpak (runtime/SDK pulled from Flathub) ==="
          # App version is derived from io.github.ashbuk.dabri.appdata.xml by the manifest.
          # --disable-rofiles-fuse: FUSE is unavailable on GitHub runners.
          flatpak-builder \
            --user \
            --install-deps-from=flathub \
            --force-clean \
            --disable-rofiles-fuse \
            --default-branch=stable \
            --repo=repo \
            build-dir io.github.ashbuk.dabri.yml

      - name: Bundle Flatpak
        run: |
          flatpak build-bundle repo dabri.flatpak io.github.ashbuk.dabri stable
          ls -lh dabri.flatpak

      - name: Upload Flatpak artifact
        uses: actions/upload-artifact@v4
        with:
          name: dabri-flatpak
          path: dabri.flatpak

      - name: Attach Flatpak bundle to Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          # Only attach the asset; the release itself (notes, title) is owned by
          # release-appimage.yml. Unspecified fields are left untouched.
          files: dabri.flatpak
          fail_on_unmatched_files: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
